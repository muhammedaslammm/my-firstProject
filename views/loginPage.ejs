<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Quicksand&family=Roboto:wght@400;500;700&display=swap"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="/styles/loginPage.css" />
    <title>login page</title>
    <style>
      body {
        font-family: "poppins", sans-serif;
        margin: 0;
        padding: 0;
      }
      header {
        border-bottom: 1px solid rgb(205, 205, 205);
        background-color: white;
        padding: 8px 0;
      }
      nav {
        height: 32px;
        width: 85%;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      .nav-left {
        display: flex;
        align-items: center;
        width: 40%;
      }
      .nav-left .logo a {
        text-decoration: none;
        color: black;
        margin-right: 5%;
      }
      .nav-right a {
        text-decoration: none;
        color: black;
        font-size: 16px;
        margin-right: 24px;
        font-weight: 500;
        transition: 0.3s;
      }
      .nav-right a:last-child {
        margin: 0;
      }
      .nav-right a:hover {
        color: rgba(32, 109, 133, 0.892);
      }
      .logo {
        margin-right: 8px;
        font-size: 22px;
        font-weight: 500;
      }
      .container {
        width: 80%;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        padding-top: 5%;
      }
      .login-picture {
        width: 50%;
        display: flex;
        justify-content: center;
        align-items: center;
      }
      .login-picture img {
        width: 60%;
        margin-right: 20%;
      }

      .login-section {
        width: 50%;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
      }
      .login-box {
        background-color: white;
        width: 98%;
        border-radius: 7px;
        padding: 15px 12px;
        box-sizing: border-box;
        border: 1px solid rgb(207, 206, 206);
      }

      .login-box label {
        display: block;
      }
      .login-box input[type="email"],
      .login-box input[type="password"] {
        width: 95%;
        outline: none;
        padding: 6px;
        border: 1px solid rgb(197, 195, 195);
      }

      input[type="search"] {
        border: 1px solid rgb(203, 203, 203);
        background-color: white;
        padding: 4px;
        border-radius: 3px;
      }
      .nav-left input[type="submit"] {
        background-color: rgba(32, 109, 133, 0.892);
        padding: 4px 7.9px;
        font-weight: 500;
        color: white;
        border-radius: 3px;
        border: none;
      }
      .login-box form input[type="submit"] {
        background-color: rgba(32, 109, 133, 0.892);
        padding: 4px 7.9px;
        font-weight: 700;
        color: white;
        border: 1px solid;
        border-radius: 2px;
      }
      .login-box form p {
        margin: 0;
        color: rgb(190, 33, 33);
        margin-bottom: 10px;
      }
      .login-box .login-error {
        margin: 0;
      }
      .signup-link p {
        font-size: 13px;
        margin: 0;
        margin-top: 10px;
      }
      input[type="submit"]:hover {
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <header>
      <nav>
        <div class="nav-left">
          <div class="logo"><a href="/">moas</a></div>
        </div>
      </nav>
    </header>
    <main>
      <div class="container">
        <div class="login-picture">
          <img src="/wallpapers/Computer login-bro.png" alt="" />
        </div>
        <div class="login-section">
          <div class="login-box">
            <div class="login-head">Login</div>
            <form action="/login" method="post">
              <label for="">email</label>
              <input
                type="email"
                name="email"
                value="<%= typeof userData != 'undefined' ? userData.email:''%>"
              />
              <p>
                <%if(typeof errors != 'undefined' && errors.email) { %>
                <%=errors.email%> <% }%>
              </p>
              <label for="">password</label>
              <input
                type="password"
                name="password"
                value="<%= typeof userData != 'undefined' ? userData.password:''%>"
              />
              <p>
                <%if(typeof errors != 'undefined' && errors.password) { %>
                <%=errors.password%> <% }%>
              </p>

              <p class="login-error">
                <%if(typeof errors != 'undefined' && errors.login) { %>
                <%=errors.login%> <% }%>
              </p>
              <input type="submit" />
            </form>
            <div class="signup-link">
              <p>create new account? <a href="/signup">register</a></p>
            </div>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
